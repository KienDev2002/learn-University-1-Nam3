
#  thư viện math
# import <tên thư viện> , muốn sử dụng hàm trong thư viện đó thì <tên thư viện>.<tên hàm>

# .trunc(x): trả về 1 số nguyên là phần nguyên của số  x
#  .floor(x): trả về số nguyên được làm tròn từ số x, kq luôn nhỏ hơn hoặc bằng x
# .ceil(x): trả về 1 só nguyên đc làm tròn số từ số x, kết quả luôn luôn lớn hơn bằng x
# .fabs(x): trả về 1 số thực là giá trị tuyệt đối của số x
# .sqrt(x): trả về 1 số thực à căn bậc 2 cảu x
# gcd(x,y): trả về 1 số nguyên là ước chung lớn nhất của 2 số x va y

# import math 
#  a = 4
#  b = 6
# c =  math.gcd(4,6)
# print("kieu la:"+c)